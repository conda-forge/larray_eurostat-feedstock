{% set name = "larray_eurostat" %}
{% set version = "0.32" %}
{% set sha256 = "c264783835b80bf7cc41a100a4f9230a6f52f6a354054285bbdfecf0fe861bd9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/larray-project/larray_eurostat/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - larray

test:
  imports:
    - larray_eurostat

about:
  home: http://github.com/larray-project/larray_eurostat
  license: GPL-3.0
  license_family: GPL
  license_file: LICENSE
  summary: "Additional package to import Eurostat files using LArray"

extra:
  recipe-maintainers:
    - alixdamman
    - gdementen
